// ----------------------------------------------------------------------------------------------------------------------------------------------------------------


const myArray = [1, 2, 3, 4, 5]

const newMyArray = myArray.map((value, index, array) => {
  return value+10;
});

console.log(newMyArray);      // [ 11, 12, 13, 14, 15 ]


// ----------------------------------------------------------------------------------------------------------------------------------------------------------------


const array = [11, 13, 15, 17, 19]

const newArray = array
                  .map((value, index, array) => { return value + 20} )
                  .filter((value, index, array) => { return value > 33} )

console.log(newArray);      // [ 35, 37, 39 ]


// ----------------------------------------------------------------------------------------------------------------------------------------------------------------